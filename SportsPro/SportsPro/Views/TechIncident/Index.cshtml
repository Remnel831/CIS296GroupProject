@model TechIncidentIndexViewModel


@{
	string title = ViewBag.Action;
	ViewBag.Title = title;
	Layout = "~/Views/Shared/_Layout_Validation.cshtml";
}

<h2>@ViewBag.Title Incident</h2>
<form asp-controller="TechIncident" asp-action="Index" method="post">
    <div>
        <select asp-for="SelectedTechnicianID" class="form-control"
                asp-items="@(new SelectList(Model.Technicians, "TechnicianID", "Name"))">
            <option value="0" disabled selected>Select a Technician</option>
            <option value="-1">Not Assigned</option>
        </select>
        <span asp-validation-for="@Model.SelectedTechnicianID" class="text-danger"></span>
    </div>

    <div class="mt-3">
        <button type="submit" class="btn btn-primary">Select</button>
    </div>

    <div class="mt-3">
        <div asp-validation-summary="ModelOnly" class="text-danger"></div>
    </div>
</form>

